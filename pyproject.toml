[build-system]
# Minimum requirements for the build system to execute.
requires = ["setuptools >= 45", "setuptools_scm[toml]>=6.2",
    "wheel", "cython", "numpy >= 1.20"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "src/numina/_version.py"
